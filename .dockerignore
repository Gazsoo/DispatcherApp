# Visual Studio temporary files (The cause of your error)
.vs/
.vscode/

# Build results (Docker will build its own)
**/bin/
**/obj/

# Frontend dependencies (Docker will npm install its own)
**/node_modules/

# Git history
.git/
.gitignore

# Docker files themselves
Dockerfile
docker-compose.yml